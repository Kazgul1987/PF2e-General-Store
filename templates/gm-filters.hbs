<form class="pf2e-general-store-gm">
  <p>General Store GM-Menü</p>
  <p>Öffne den General Store für den aktuell ausgewählten Token oder deinen Charakter.</p>

  <div class="form-group">
    <label for="pf2e-general-store-traits">Traits (Mehrfachauswahl)</label>
    <input
      id="pf2e-general-store-traits"
      type="text"
      name="gm-traits"
      placeholder="z.B. alchemical, consumable, fire"
      value="{{traitsInput}}"
    />
    <p class="hint">Mehrere Traits mit Komma trennen.</p>
  </div>

  <div class="form-group">
    <label for="pf2e-general-store-min-level">Min. Itemlevel</label>
    <input
      id="pf2e-general-store-min-level"
      type="number"
      name="min-level"
      placeholder="Keine"
      value="{{minLevel}}"
    />
  </div>

  <div class="form-group">
    <label for="pf2e-general-store-max-level">Max. Itemlevel</label>
    <input
      id="pf2e-general-store-max-level"
      type="number"
      name="max-level"
      placeholder="Keine"
      value="{{maxLevel}}"
    />
  </div>

  <div class="form-group">
    <label for="pf2e-general-store-rarity">Seltenheit</label>
    <select id="pf2e-general-store-rarity" name="rarity">
      {{#each rarityOptions}}
        <option value="{{this.value}}" {{#if this.selected}}selected{{/if}}>
          {{this.label}}
        </option>
      {{/each}}
    </select>
  </div>

  <hr />

  <div class="form-group">
    <label class="store-option">
      <input type="checkbox" name="bulk-active" />
      <span>Sammelbestellung aktivieren</span>
    </label>
    <p class="hint">
      Wenn aktiv, werden Käufe gesammelt und erst nach GM-Bestätigung ausgeführt.
    </p>
  </div>

  <section class="bulk-order__gm" data-bulk-order-gm>
    <div class="bulk-order__gm-header">
      <strong>Sammelbestellung</strong>
      <span class="bulk-order__gm-total">0 gp</span>
    </div>
    <ul class="bulk-order__gm-list"></ul>
    <button class="bulk-order__gm-confirm" type="button">
      Sammelbestellung bestätigen
    </button>
  </section>
</form>
